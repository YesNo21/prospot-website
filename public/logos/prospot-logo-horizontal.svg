<svg viewBox="0 0 500 100" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Horizontal logo with dot on the right -->
  <g id="prospot-horizontal">
    <!-- PRO text with rounded rectangle -->
    <rect x="10" y="25" width="120" height="50" rx="25" fill="currentColor"/>
    <!-- Circle dot -->
    <circle cx="155" cy="50" r="25" fill="currentColor"/>
    <!-- PROSPOT text -->
    <text x="200" y="65" font-family="Arial, sans-serif" font-size="48" font-weight="bold" fill="currentColor">PROSPOT</text>
  </g>
</svg>